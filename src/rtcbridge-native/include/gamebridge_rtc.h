// The native fallback consumes exactly the same versioned public ABI.
// The builder export preserves this relative path and the canonical header.
#include "../../rtcbridge/include/gamebridge_rtc.h"
